> ghost spawn
> ghost eating
> collision detection
> play precomputed sound
> fm sound
> effect generation
> minimal mat4 reimplementation
> glitch animated texture effect
> neon blending effect
> map/path generator
> render cooling
------------------------------------------
V ghost model
V objects independent translation/rotation
V signals
V semi-transparency in generated textures
V refactor blocks to entities model
